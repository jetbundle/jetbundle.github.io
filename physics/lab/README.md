# physics / lab — isolated deploy tests

Pages under `physics/lab/` are **not linked** from `physics/index.html` or other public nav by design. They form their own link graph for drafts (symplect-aligned stat-mech spine, etc.).

Enter a cluster via direct URL, e.g. `physics/lab/emergent-invariance/`.
